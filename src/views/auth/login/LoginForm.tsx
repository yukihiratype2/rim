import { Button, TextField } from '@material-ui/core';

const LoginForm = () => (
  <form>
    <TextField variant="outlined" placeholder="Username" />
    <TextField variant="outlined" placeholder="Username" type="password" />
    <Button />
  </form>
);

export default LoginForm;
